<header class="navbar">
  <div class="logo">💰 CashMash</div>
  <div class="nav-buttons">
    <% if(user){ %>
      <a href="/dashboard" class="btn gold">Dashboard</a>
      <a href="/logout" class="btn outline">Logout</a>
    <% } else { %>
      <a href="/login" class="btn gold">Login</a>
      <a href="/register" class="btn outline">Sign Up</a>
    <% } %>
  </div>
</header>
